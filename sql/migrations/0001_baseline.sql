-- Baseline marker -- see sql/migrations/README.md.
-- Represents sql/schema.sql plus the pre-existing ad hoc scripts in
-- sql/ as of 2026-08-27, all already live. Intentionally a no-op;
-- future migrations number from 0002.
SELECT 1;
